# Tests

Unit and integration checks currently live at the repository root for
compatibility with the existing scripts.

Canonical checks:

- `test_lsl.py`
- `test_semantic_sdr_integration.py`
- `verify_sdr_binary_encoding.py`

These validate the core package behavior before the benchmarks run.

